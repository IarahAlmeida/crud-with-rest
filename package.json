{
    "name": "crud-with-rest-server",
    "version": "1.0.0",
    "engines": {
        "node": "12.18.4",
        "npm": "6.14.8"
    },
    "scripts": {
        "start": "node server.js",
        "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm audit fix && npm run build"
    },
    "dependencies": {
        "express": "^4.16.4",
        "sqlite3": "^5.0.2"
    },
    "devDependencies": {}
}
